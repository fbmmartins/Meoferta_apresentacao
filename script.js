const totalSlides=13;let currentSlide=1;const base='./slides/';const slideImage=document.getElementById('slideImage');const slideMenu=document.getElementById('slideMenu');const screenBtn=document.getElementById('screenBtn');function pad(n){return String(n).padStart(2,'0')}function fileName(n){return 'ap'+pad(n)+'.png'}function pathName(n){return base+fileName(n)}function buildMenu(){for(let i=1;i<=totalSlides;i++){const btn=document.createElement('button');btn.type='button';btn.textContent='AP'+pad(i);btn.dataset.slide=String(i);btn.addEventListener('click',()=>goToSlide(i));slideMenu.appendChild(btn)}}function updateMenu(){document.querySelectorAll('.slide-menu button').forEach((btn)=>btn.classList.toggle('active',Number(btn.dataset.slide)===currentSlide))}function updateSlide(){slideImage.classList.add('is-changing');const n=currentSlide;setTimeout(()=>{slideImage.src=pathName(n);slideImage.alt='Slide AP'+pad(n);updateMenu();slideImage.classList.remove('is-changing')},100)}function goToSlide(n){if(n<1)n=totalSlides;if(n>totalSlides)n=1;currentSlide=n;updateSlide()}function preload(){for(let i=1;i<=totalSlides;i++){const img=new Image();img.src=pathName(i)}}function updateFullscreenButton(){screenBtn.textContent=document.fullscreenElement?'Sair':'Tela cheia'}document.addEventListener('keydown',(event)=>{if(event.key==='ArrowRight')goToSlide(currentSlide+1);if(event.key==='ArrowLeft')goToSlide(currentSlide-1);if(event.key==='Home')goToSlide(1);if(event.key==='End')goToSlide(totalSlides)});screenBtn.addEventListener('click',()=>{const el=document.documentElement;if(!document.fullscreenElement&&el.requestFullscreen){el.requestFullscreen()}else if(document.exitFullscreen){document.exitFullscreen()}});document.addEventListener('fullscreenchange',updateFullscreenButton);buildMenu();preload();updateSlide();updateFullscreenButton();